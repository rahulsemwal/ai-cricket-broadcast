{
  "name": "ai-cricket-broadcast-root",
  "version": "1.0.0",
  "scripts": {
    "install": "cd server && npm install",
    "build": "rm -rf build && mkdir build && cp -r client/* build/",
    "start": "cd server && npm start"
  }
}